import { colors } from './colors';
import { sizes, space, lineHeights, fonts, fontWeights, fontSizes } from './Text';

export const theme = {
  colors,
  sizes,
  space,
  lineHeights,
  fonts,
  fontWeights,
  fontSizes,
};
